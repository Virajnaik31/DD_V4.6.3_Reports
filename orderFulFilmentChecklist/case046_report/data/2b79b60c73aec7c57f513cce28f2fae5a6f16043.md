# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: orderFulFilmentChecklist\case-046.spec.ts >> Case 046 - Full Order Fulfilment Flow
- Location: tests\orderFulFilmentChecklist\case-046.spec.ts:13:5

# Error details

```
Error: locator.click: Target page, context or browser has been closed
Call log:
  - waiting for getByRole('link', { name: 'Out-Station Order Shipped', exact: true })
    - locator resolved to <a tabindex="0" href="/oft/out-station-order-shipped" class="MuiButtonBase-root MuiListItemButton-root MuiListItemButton-dense MuiListItemButton-gutters MuiListItemButton-root MuiListItemButton-dense MuiListItemButton-gutters css-znkeek">…</a>
  - attempting click action
    2 × waiting for element to be visible, enabled and stable
      - element is not stable
    - retrying click action
    - waiting 20ms
    - waiting for element to be visible, enabled and stable
  - element was detached from the DOM, retrying

```

# Test source

```ts
  747 | 
  748 |     const printManifestBtn = page.getByRole('button', { name: 'PRINT MANIFEST' });
  749 |     if (await printManifestBtn.isDisabled().catch(() => false)) {
  750 |         await matchRow2.locator('td').first().click({ force: true }).catch(() => { });
  751 |         await chk2.click({ force: true }).catch(() => { });
  752 |     }
  753 | 
  754 |     await screenshot('oft_print_manifest_searched');
  755 | 
  756 |     const printManifestPromise = page.waitForEvent('popup', { timeout: 5000 }).catch(() => null);
  757 |     await printManifestBtn.click();
  758 |     const printManifestPage = await printManifestPromise;
  759 |     if (printManifestPage && !printManifestPage.isClosed()) {
  760 |         await printManifestPage.waitForLoadState('domcontentloaded').catch(() => { });
  761 |         await printManifestPage.waitForTimeout(1000);
  762 |         await printManifestPage.close().catch(() => { });
  763 |     }
  764 |     await page.bringToFront().catch(() => { });
  765 |     await page.waitForTimeout(2500); // Allow status transition to commit
  766 | 
  767 |     // Transition to Out-Station Order Pickup Scheduled
  768 |     await page.getByRole('link', { name: 'Dashboard' }).click().catch(() => { });
  769 |     const orderShippedBtn = page.getByRole('button', { name: 'Order Shipped Status' });
  770 |     if (await orderShippedBtn.isVisible().catch(() => false)) {
  771 |         await orderShippedBtn.click().catch(() => { });
  772 |     }
  773 |     const outStationPickupScheduledLink = page.getByRole('link', { name: 'Out-Station Order Pickup Scheduled', exact: true });
  774 |     await outStationPickupScheduledLink.scrollIntoViewIfNeeded().catch(() => { });
  775 |     await outStationPickupScheduledLink.click();
  776 | 
  777 |     // Robust search & wait loop for the order to appear in Pickup Scheduled
  778 |     let pickupRowFound = false;
  779 |     for (let attempt = 0; attempt < 6; attempt++) {
  780 |         await page.getByRole('textbox', { name: 'Search' }).fill('');
  781 |         await page.waitForTimeout(300);
  782 |         await page.getByRole('textbox', { name: 'Search' }).fill(intentId);
  783 |         await page.waitForTimeout(1000);
  784 | 
  785 |         const row = page.locator('tbody tr, table tr').filter({ hasText: intentId }).first();
  786 |         if (await row.isVisible().catch(() => false)) {
  787 |             pickupRowFound = true;
  788 |             break;
  789 |         }
  790 | 
  791 |         const revalidateBtn = page.getByRole('button', { name: /Revalidate Cache/i });
  792 |         if (await revalidateBtn.isVisible().catch(() => false)) {
  793 |             await revalidateBtn.click().catch(() => { });
  794 |             await page.waitForTimeout(1500);
  795 |         } else {
  796 |             await page.reload({ waitUntil: 'domcontentloaded' }).catch(() => { });
  797 |             await page.waitForTimeout(1500);
  798 |         }
  799 |     }
  800 | 
  801 |     await screenshot('oft_shipped_searched');
  802 | 
  803 |     const targetPickupRow = page.locator('tbody tr, tr').filter({ hasText: intentId }).first();
  804 |     await targetPickupRow.waitFor({ state: 'visible', timeout: 15000 });
  805 | 
  806 |     const chkInput = targetPickupRow.locator('input[type="checkbox"]').first();
  807 |     if (await chkInput.count() > 0) {
  808 |         const isChecked = await chkInput.isChecked().catch(() => false);
  809 |         if (!isChecked) {
  810 |             await chkInput.check({ force: true }).catch(() => { });
  811 |         }
  812 |     } else {
  813 |         const pickupChk = targetPickupRow.getByRole('checkbox').or(targetPickupRow.locator('.MuiCheckbox-root')).first();
  814 |         await pickupChk.click({ force: true }).catch(() => { });
  815 |     }
  816 | 
  817 |     // Verify checkbox is checked, if still not checked click the MUI checkbox span
  818 |     const chkStillUnchecked = !(await targetPickupRow.locator('input[type="checkbox"]').isChecked().catch(() => false));
  819 |     if (chkStillUnchecked) {
  820 |         const muiSpan = targetPickupRow.locator('.MuiCheckbox-root, svg').first();
  821 |         await muiSpan.click({ force: true }).catch(() => { });
  822 |     }
  823 | 
  824 |     const shippedBtn = page.getByRole('button', { name: /^SHIPPED$/i })
  825 |         .or(page.locator('button').filter({ hasText: /^SHIPPED$/i }))
  826 |         .first();
  827 |     await expect(shippedBtn).toBeEnabled({ timeout: 15000 });
  828 |     await shippedBtn.click();
  829 | 
  830 |     const okShipped = page.getByRole('button', { name: 'Ok' }).or(page.getByRole('button', { name: 'OK' })).or(page.getByRole('button', { name: /OK/i })).first();
  831 |     await okShipped.waitFor({ state: 'visible', timeout: 10000 }).catch(() => { });
  832 |     if (await okShipped.isVisible().catch(() => false)) {
  833 |         await screenshot('oft_shipped_success');
  834 |         await okShipped.click().catch(() => { });
  835 |         await page.locator('.MuiBackdrop-root, [role="dialog"]').first().waitFor({ state: 'detached', timeout: 10000 }).catch(() => { });
  836 |     }
  837 |     await page.waitForTimeout(1500);
  838 | 
  839 |     // Transition to Out-Station Order Shipped
  840 |     await page.getByRole('link', { name: 'Dashboard' }).click().catch(() => { });
  841 |     const orderShippedMenuBtn = page.getByRole('button', { name: 'Order Shipped Status' });
  842 |     if (await orderShippedMenuBtn.isVisible().catch(() => false)) {
  843 |         await orderShippedMenuBtn.click().catch(() => { });
  844 |     }
  845 |     const outStationShippedLink = page.getByRole('link', { name: 'Out-Station Order Shipped', exact: true });
  846 |     await outStationShippedLink.scrollIntoViewIfNeeded().catch(() => { });
> 847 |     await outStationShippedLink.click();
      |                                 ^ Error: locator.click: Target page, context or browser has been closed
  848 |     await page.getByRole('textbox', { name: 'Search' }).fill(intentId);
  849 |     await screenshot('oft_order_shipped_searched');
  850 | 
  851 |     const viewDetailsShipped = page.locator('table tbody tr, tbody tr').filter({ hasText: intentId }).getByRole('button', { name: /View Details/i }).first().or(page.getByRole('button', { name: 'View Details' }).first());
  852 |     await viewDetailsShipped.waitFor({ state: 'visible', timeout: 15000 });
  853 |     await viewDetailsShipped.scrollIntoViewIfNeeded().catch(() => { });
  854 |     await viewDetailsShipped.click();
  855 |     await screenshot('oft_order_shipped_details');
  856 | 
  857 |     const deliveredStatusCombo = page.getByRole('combobox', { name: /Status/i })
  858 |         .or(page.locator('.MuiSelect-select'))
  859 |         .or(page.getByLabel('', { exact: true }))
  860 |         .first();
  861 |     await deliveredStatusCombo.click();
  862 |     const deliveredOpt = page.getByRole('option', { name: 'Delivered' }).first();
  863 |     await deliveredOpt.click();
  864 |     await screenshot('oft_order_delivered_selected');
  865 | 
  866 |     await page.getByRole('button', { name: 'SUBMIT' }).click();
  867 |     const okDelivered = page.getByRole('button', { name: 'OK' }).or(page.getByRole('button', { name: 'Ok' }));
  868 |     await okDelivered.waitFor({ state: 'visible' });
  869 |     await screenshot('oft_order_delivered_success');
  870 |     await okDelivered.click();
  871 | 
  872 |     // Transition to Orders Delivered By Courier
  873 |     await page.getByRole('link', { name: 'Dashboard' }).click().catch(() => { });
  874 |     const ordersDeliveredBtn = page.getByRole('button', { name: 'Orders Delivered' });
  875 |     if (await ordersDeliveredBtn.isVisible().catch(() => false)) {
  876 |         await ordersDeliveredBtn.click().catch(() => { });
  877 |     }
  878 |     const deliveredByCourierLink = page.getByRole('link', { name: 'Orders Delivered By Courier', exact: true });
  879 |     await deliveredByCourierLink.scrollIntoViewIfNeeded().catch(() => { });
  880 |     await deliveredByCourierLink.click();
  881 |     await page.getByRole('textbox', { name: 'Search' }).fill(intentId);
  882 |     await screenshot('oft_order_delivered_list');
  883 | 
  884 |     const viewDetailsDelivered = page.locator('table tbody tr, tbody tr').filter({ hasText: intentId }).getByRole('button', { name: /View Details/i }).first().or(page.getByRole('button', { name: 'View Details' }).first());
  885 |     await viewDetailsDelivered.waitFor({ state: 'visible', timeout: 15000 });
  886 |     await viewDetailsDelivered.scrollIntoViewIfNeeded().catch(() => { });
  887 |     await viewDetailsDelivered.click();
  888 |     await page.getByRole('heading', { name: 'SHIPPING / BILLING ADDRESS' }).click();
  889 |     await page.getByRole('heading', { name: 'REMARK LOG' }).click();
  890 |     await screenshot('oft_order_delivered_final_details');
  891 | });
```